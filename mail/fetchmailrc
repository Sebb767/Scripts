# in ~/.fetchmailrc

defaults
        auth password
        protocol imap
        # maildrop is self-compiled with --prefix=$HOME
        mda "$HOME/bin/maildrop -d %T"
        keep

poll imap.novell.com
        username ""
        password ""
        # to fetch mails from a specific folder
        #folder "INBOX/Bugzilla"
